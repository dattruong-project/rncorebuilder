export declare const setUpLocale: (locale: string) => string;
export declare const formatDate: (date: Date, format: string) => string;
export declare const parseDate: (dateString: string, format: string) => Date | null;
